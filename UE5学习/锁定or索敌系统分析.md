# 锁定or索敌系统分析


1. 检测角色前方范围内的所有敌人
2. 对所有敌人做可见性检测
3. 保留可见的所有人敌人
4. 从中选取距离镜头中心最近的敌人作为锁定目标（应该是距离角色越近，锁定优先级越高）
5. 不断对锁定的敌人进行射线检测，看不见则取消锁定（可以设置粘滞时间）
6. 左右切换索敌对象时，应该怎么做才能保证性能最优呢？

### 优化

1. 减少射线检测的次数
2. 每次选择出一个最优的满足条件的Actor
3. 对于后续的Actor首先判断条件满足，再与该Actor进行优先级比较

