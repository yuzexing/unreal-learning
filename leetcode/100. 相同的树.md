# 100. 相同的树

### 初次提交
```
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */
class Solution {
public:
    bool isSameTree(TreeNode* p, TreeNode* q) {
        if ((!q && p) || (q && !p)) {
            return false;
        }
        if (!p && !q) return true;

        stack<TreeNode*> sk_p;
        stack<TreeNode*> sk_q;
        sk_p.push(p);
        sk_q.push(q);
        while (!sk_p.empty() || !sk_q.empty()) {
            TreeNode* root_p = sk_p.top();
            TreeNode* root_q = sk_q.top();
            sk_p.pop();
            sk_q.pop();
            if (root_p->val != root_q->val) {
                return false;
            }
            if ((!root_p->left && root_q->left) || (root_p->left && !root_q->left)) {
                return false;
            }
            if ((!root_p->right && root_q->right) || (root_p->right && !root_q->right)) {
                return false;
            }
            if (root_p->left && root_q->left) {
                sk_p.push(root_p->left);
                sk_q.push(root_q->left);
            }
            if (root_p->right && root_q->right) {
                sk_p.push(root_p->right);
                sk_q.push(root_q->right);
            }
        }
        return sk_p.empty() && sk_q.empty();
    }
};
```
### 结果
<img width="872" height="694" alt="image" src="https://github.com/user-attachments/assets/0f438992-96a8-48a8-8d14-d70414534d8c" />

### 分析
时间复杂度O(n)，空间复杂度O(n)
